//Eloquent Chess Board Exercise
var size = 8;
var num = '';
for (var i = 0; i < size; i++) {
	for (var x = 0; x < size; x++) {
		if ((i+x) % 2 == 0)
			num += " ";
		else 
			num += "#";
	}
	num += "\n";
}

console.log(num);
